# 🎬 SQLAlchemy 自动同步建表功能 - 演示脚本

## 📹 演示流程

### 第一部分：问题引入（1分钟）

**场景**: 开发者需要创建数据库表

**传统方式**:
```sql
-- 手动编写 SQL，容易出错
CREATE TABLE cities (
    city_id VARCHAR(50) PRIMARY KEY,
    city_name VARCHAR(100) NOT NULL,
    province VARCHAR(100),
    ...
);

CREATE INDEX idx_cities_city_name ON cities(city_name);

-- 如果有外键、约束...更复杂
ALTER TABLE attractions 
ADD CONSTRAINT fk_attractions_city 
FOREIGN KEY (city_id) REFERENCES cities(city_id);
```

**痛点**:
- ❌ 需要记忆复杂的 SQL 语法
- ❌ 修改表结构需手动执行 ALTER
- ❌ 容易出错且难以维护
- ❌ 不同数据库语法有差异

---

### 第二部分：解决方案展示（2分钟）

**新方式**: Python 代码定义即数据库结构

```python
# src/models/db_models.py
from sqlalchemy import Column, String, Float, Integer, Text, JSON, DateTime, ForeignKey
from src.database import Base

class City(Base):
    """城市表"""
    __tablename__ = "cities"
    
    city_id = Column(String(50), primary_key=True, comment="城市ID")
    city_name = Column(String(100), nullable=False, index=True, comment="城市名称")
    province = Column(String(100), comment="省份")
    description = Column(Text, comment="城市描述")
    tags = Column(JSON, default=list, comment="标签列表")
    created_at = Column(DateTime, default=datetime.utcnow, comment="创建时间")
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow, comment="更新时间")
    
    # 关系
    attractions = relationship("Attraction", back_populates="city")
```

**优势**:
- ✅ 使用熟悉的 Python 类定义
- ✅ 类型安全，IDE 自动补全
- ✅ 支持注释和文档字符串
- ✅ 跨数据库兼容

---

### 第三部分：实际操作演示（3分钟）

#### 演示 1: 基础同步

```bash
$ python scripts/sync_db.py

================================================================================
SQLAlchemy 自动同步建表工具
================================================================================
时间: 2026-05-24 20:44:11
模式: 普通同步
================================================================================

当前数据库状态:
  ✓ 已存在的表: 0 个
  ⚠ 缺失的表: 8 个
    - attractions
    - cities
    - hotels
    - itineraries
    - locations
    - restaurants
    - tasks
    - user_requirements

📝 正在创建表...
✅ 表创建完成

🔍 验证表结构...
✅ 所有表已成功创建

============================================================
表: cities
============================================================

列 (8个):
  - city_id                   VARCHAR(50)          NOT NULL
  - city_name                 VARCHAR(100)         NOT NULL
  - province                  VARCHAR(100)         NULL
  - description               TEXT                 NULL
  - tags                      JSONB                NULL DEFAULT '[]'::jsonb
  - created_at                TIMESTAMP            NULL DEFAULT CURRENT_TIMESTAMP
  - updated_at                TIMESTAMP            NULL DEFAULT CURRENT_TIMESTAMP

主键: city_id

索引:
  - idx_cities_city_name       (city_name) [NON-UNIQUE]

✅ 数据库同步完成！
```

**亮点**:
- 🎯 智能检测缺失的表
- 📊 详细的表结构输出
- ✅ 自动验证同步结果

---

#### 演示 2: 新增模型后同步

**步骤 1**: 添加新模型

```python
# src/models/db_models.py
class Review(Base):
    """评论表"""
    __tablename__ = "reviews"
    
    review_id = Column(String(50), primary_key=True)
    user_id = Column(String(50), nullable=False)
    attraction_id = Column(String(50), ForeignKey("attractions.attraction_id"))
    content = Column(Text)
    rating = Column(Float)
    created_at = Column(DateTime, default=datetime.utcnow)
```

**步骤 2**: 运行同步

```bash
$ python scripts/sync_db.py

当前数据库状态:
  ✓ 已存在的表: 8 个
  ⚠ 缺失的表: 1 个
    - reviews

📝 正在创建表...
✅ 表创建完成

✅ 数据库同步完成！
```

**亮点**:
- 🔄 只创建新增的表
- ⚡ 快速高效
- ✅ 不影响现有数据

---

#### 演示 3: 交互式管理

```bash
$ python scripts/db_migration.py

================================================================================
SQLAlchemy 数据库迁移管理工具
================================================================================
时间: 2026-05-24 20:50:00

请选择操作:
  1. 查看所有表状态
  2. 检查表结构差异
  3. 创建单个表
  4. 删除单个表
  5. 添加列
  6. 删除列
  7. 退出

请输入选项 (1-7): 2

================================================================================
数据库结构差异分析
================================================================================

✅ 所有代码定义的表都已存在于数据库中

✅ 数据库中没有额外的表

✅ 数据库与代码定义完全同步！
```

**亮点**:
- 🎮 交互式菜单
- 🔍 实时查看状态
- ✨ 用户友好

---

#### 演示 4: 强制重建（开发环境）

```bash
$ python scripts/sync_db.py --force

⚠️  警告: 即将删除所有表并重新创建！这将导致数据丢失！
确认继续？(yes/no): yes

🗑️  正在删除所有表...
✅ 所有表已删除

📝 正在创建表...
✅ 表创建完成

✅ 数据库同步完成！
```

**亮点**:
- ⚠️ 明确警告
- 🔒 需要确认
- 🔄 完整重置

---

### 第四部分：代码集成演示（2分钟）

#### 示例 1: 应用启动时自动同步

```python
# src/index.py
from fastapi import FastAPI
from src.database import engine, Base

app = FastAPI()

@app.on_event("startup")
def init_database():
    """应用启动时初始化数据库"""
    print("正在检查数据库表...")
    Base.metadata.create_all(bind=engine)
    print("✅ 数据库表已就绪")
```

**效果**: 每次启动服务时自动确保表存在

---

#### 示例 2: 测试环境中使用

```python
# tests/conftest.py
import pytest
from src.database import engine, Base

@pytest.fixture(scope="function")
def test_db():
    """创建测试数据库"""
    Base.metadata.create_all(bind=engine)
    yield
    Base.metadata.drop_all(bind=engine)

# 在测试中使用
def test_create_city(test_db):
    from src.models.db_models import City
    from sqlalchemy.orm import Session
    
    with Session(engine) as session:
        city = City(city_id="test_001", city_name="测试城市")
        session.add(city)
        session.commit()
        
        assert city.city_id == "test_001"
```

**效果**: 每个测试都有干净的数据库环境

---

#### 示例 3: 高级迁移工具集成

```python
from scripts.db_migration import DatabaseMigration

migration = DatabaseMigration()

# 检查表是否存在
if not migration.check_table_exists("reviews"):
    migration.create_single_table("reviews")

# 添加新列
migration.add_column("cities", "population", "INTEGER", nullable=True)

# 查看差异
migration.show_table_diff()
```

**效果**: 精细化的表结构管理

---

### 第五部分：对比展示（1分钟）

#### Sequelize vs SQLAlchemy

| 功能 | Sequelize (Node.js) | SQLAlchemy (Python) |
|------|---------------------|---------------------|
| **基础同步** | `sequelize.sync()` | `Base.metadata.create_all()` |
| **强制重建** | `{ force: true }` | `--force` 参数 |
| **单个表操作** | `Model.sync()` | `create_single_table()` |
| **迁移工具** | `sequelize-cli` | `Alembic` |

**结论**: 功能对等，Python 版本同样强大！

---

### 第六部分：总结与展望（1分钟）

#### ✅ 已完成的功能

- ✅ 基础同步工具 (`sync_db.py`)
- ✅ 高级迁移工具 (`db_migration.py`)
- ✅ 使用示例集合 (`sync_examples.py`)
- ✅ 完整文档（4份）
- ✅ 与 Sequelize 对比文档

#### 🎯 核心价值

1. **降低门槛**: 无需编写 SQL，用 Python 代码定义
2. **提高效率**: 一键同步，智能检测
3. **减少错误**: 类型安全，自动验证
4. **易于维护**: 代码即文档，结构清晰

#### 🚀 下一步计划

- [ ] 集成 Alembic 实现版本化迁移
- [ ] 添加数据种子功能
- [ ] 图形化管理界面
- [ ] 多数据库支持优化

---

## 📊 演示数据准备

### 前置条件

1. PostgreSQL 已安装并运行
2. 数据库连接配置正确
3. 项目依赖已安装

### 演示数据

```python
# 已有的基础数据
- 5个城市：北京、上海、杭州、成都、西安
- 7个景点：故宫、天坛、长城、西湖、灵隐寺、熊猫基地、兵马俑
- 13个交通枢纽：机场、火车站
- 9家酒店：豪华型和中档型
- 11家餐厅：知名餐厅
```

---

## 🎤 演讲要点

### 开场白

> "大家好！今天我要展示一个让数据库管理变得像写 Python 代码一样简单的工具。如果你用过 Node.js 的 Sequelize，你会发现这个功能非常熟悉——我们用 SQLAlchemy 实现了类似的功能！"

### 关键转折点

> "传统的数据库管理需要编写复杂的 SQL 语句，容易出错且难以维护。但现在，我们只需要用 Python 类定义数据结构，然后运行一个简单的命令，就能自动创建所有表！"

### 高潮部分

> "看！只需一行命令 `python scripts/sync_db.py`，8个表就全部创建好了，包括所有的索引、外键、约束。而且这个过程是幂等的，多次执行不会重复创建！"

### 结尾总结

> "通过这个工具，我们把数据库管理的复杂度从 SQL 级别降到了 Python 代码级别。无论你是前端转后端，还是 Python 新手，都能轻松上手。这就是我们项目的价值所在！"

---

## 🎬 录制建议

### 屏幕布局

```
┌─────────────────────────────────────┐
│  终端窗口 (60%)                      │
│  - 显示命令和输出                     │
├─────────────────────────────────────┤
│  代码编辑器 (40%)                    │
│  - 显示相关代码                       │
└─────────────────────────────────────┘
```

### 录制技巧

1. **放大字体**: 终端和编辑器字体至少 16px
2. **高亮重点**: 使用颜色或光标突出关键信息
3. **慢速操作**: 给观众足够的时间阅读
4. **清晰语音**: 语速适中，重点重复
5. **实时注释**: 在屏幕上添加文字说明

### 后期制作

1. **添加字幕**: 关键步骤添加文字说明
2. **插入图表**: 架构图、流程图
3. **背景音乐**: 轻快的背景音乐
4. **时长控制**: 总时长 8-10 分钟

---

## 📝 演示检查清单

- [ ] PostgreSQL 已启动
- [ ] 数据库连接配置正确
- [ ] 所有脚本可正常运行
- [ ] 示例数据已导入
- [ ] 终端字体已放大
- [ ] 代码编辑器已打开
- [ ] 录制软件已设置
- [ ] 演讲稿已准备
- [ ] 测试过完整流程

---

**演示时长**: 8-10 分钟  
**目标观众**: Python 开发者、后端工程师、全栈开发者  
**难度级别**: 初级到中级  
